# v2.4.2

[English](v2.4.2.md)

仅基于 main 的修复候选包，原生构建源码为 `7981406ad532d2f730b062c0d5900242e3d5f658`。包含 v2.4.1 之后已合入 main 的变更，以及下述质量修复，不引入未合并的功能分支。

## 变更

- 保留 Go 公开目录无凭据获取和动态模型别名。目录回归测试验证只发送一次 GET，且不携带 Authorization、API Key、Cookie 或请求体。
- 保留显式协议启用策略：未知模型 ID 不代表支持 Chat。回归覆盖 Auto、ForceOn、ForceOff 切换，官方文档不可用时重置不改变配置，以及显式启用的代理路由被移除后的行为。
- 修正过期的模型列表与协议测试夹具，按当前运行时目录验证。缺少价格条目不能隐藏已启用的 Go 模型；禁用的协议不能变为可路由状态。
- 修复阻塞发版的未使用导入和切片成员判断 lint 问题。默认测试命令补入现有工具链回归测试。
- package、Rust、Tauri 和 Compose 版本同步至 2.4.2。工作区锁文件由 Cargo 生成，校验确认外部依赖没有变更。

## 验证与构建产物

修复源码已通过全部现有 Web、Rust 工作区和 Windows Tauri Quality 检查。工具链测试、V3/V4 契约检查与发布输入校验也已在[准备工作流](https://github.com/klarkxy/open-console-gateway/actions/runs/35306960682)通过。

Windows x64、macOS Universal 和 Linux x64 候选包由未修改的[跨平台发布工作流](https://github.com/klarkxy/open-console-gateway/actions/runs/35307420666)构建。原生构建、打包后冒烟及产物状态以该工作流结果为准；调度成功本身不代表安装包构建成功。

临时准备工作流和修复脚本已在修复验证通过、版本提交进入 main 后移除，不作为长期发布流程保留。

## 发布边界

本次为手动调度的候选包，并非已发布的正式版，不包含生产更新签名。准备流程没有创建 v2.4.2 tag、发布 GitHub Release、更新稳定版升级通道或推送 GHCR 镜像。正式版仍须使用原有 annotated tag 流程，签名与发布门禁未改动。

尚未进行真实客户端协议检查或人工桌面操作验证，不能将自动化冒烟结果表述为人工验证。Compose 的 2.4.2 镜像引用须等对应容器版本发布后才能使用。
